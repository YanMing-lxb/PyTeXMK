- [x] `latexdiff.py` 的 `generate_diff` 方法中，文件路径检测逻辑已修复：仅当文件不存在且加 `.tex` 后缀后存在时才补全
- [x] `latexdiff.py` 的 `compile_diff` 方法不再使用 `os.chdir` 改变全局状态
- [x] `log_analysis.py` 的 `_parse_blgfile` 中 Biber 检测通过特征模式（`INFO - `、`WARN - `）判定
- [x] `log_analysis.py` 的多行错误消息解析正确收集跨行内容
- [x] `compile.py` 中旧方法（bib_judgment 等）已添加 deprecation 警告
- [x] `compile.py` 中 `_analyze_logs` 和 `_analyze_logs_update_state` 已合并
- [x] `compile.py` 智能补编循环在达到上限时输出警告并停止
- [x] `__main__.py` 配置合并逻辑已优化，优先级明确
- [x] `__main__.py` 中 `folder` 和 `output` 节的重复覆盖已移除
- [x] `__main__.py` 中 `quiet_mode`/`non_quiet` 互转逻辑已统一
- [x] `src/pytexmk/console.py` 模块已创建，提供统一的 `get_console()` 函数
- [x] `compile.py`、`latexdiff.py`、`log_analysis.py`、`engine_detect.py` 使用统一 Console
- [x] `engine_detect.py` 模块级 `MFO` 全局变量已移除
- [x] i18n 翻译覆盖已补充，.pot 和 .mo 已更新
- [x] 全部 327 个单元测试通过（6 skipped）
- [x] Lint 检查：ruff 已自动修复 309 个问题，剩余 109 个为预存风格问题（BLE001, S110, SIM102 等）
- [x] 构建成功（wheel + sdist）