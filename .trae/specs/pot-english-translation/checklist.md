- [x] 所有 17 个 .pot 文件中无空 `msgstr`（header 除外）
- [x] 所有 `#, python-format` 条目的占位符在 msgid 和 msgstr 中完全匹配
- [x] `additional.pot` 中 `%(args)s`、`%(size).3f` 占位符正确保留
- [x] `check_version.pot` 中 `%(args)s` 占位符正确保留
- [x] `run.pot` 中 `%(args1)s` 占位符名称一致
- [x] `config.pot` 中"加载"与"创建"语义不再混淆
- [x] `config.pot` 中"存在以下问题"不再译为 "does not exist"
- [x] `config.pot` 中"更新"不再译为 "loaded"
- [x] `__main__.pot` 中"日志分析失败"不再译为 "log analyzer"
- [x] 无多余的冒号或句号（msgid 无冒号时 msgstr 也不应有）
- [x] 所有非 header 条目的 `#, fuzzy` 标记已移除
- [x] `msgfmt --check` 对所有 .po/.pot 文件无报错
- [x] .mo 文件已重新编译生成
- [x] 单元测试全部通过（`make test`，327 passed）
