[build-system]
requires = ["setuptools>=83.0", "build>=1.5.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pytexmk"
dynamic = ["version"]
authors = [
  { name = "焱铭", email = "lxb-yanming@foxmail.com" },
]
description = "LaTeX 辅助编译命令行工具 LaTeX Auxiliary Compilation Command Line Tool"
keywords = ["LaTeX", "build", "latexmk"]
dependencies = [
  "rich>=15.0.0",
  "rich_argparse>=1.8.0",
  "pypdf>=6.14.2",
  "tomli-w>=1.2.0",
  "packaging>=26.2",
  "watchdog>=6.0.0",
  "platformdirs>=4.11.0",
]
readme = "README.md"
requires-python = ">=3.14"
license = "GPL-3.0-or-later"
classifiers = [
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/YanMing-lxb/PyTeXMK"
"Bug Tracker" = "https://github.com/YanMing-lxb/PyTeXMK/issues"

[project.scripts]
pytexmk = "pytexmk.__main__:main"

[project.optional-dependencies]
dev = [
  "pytest>=9.1.1",
  "pytest-cov>=7.1.0",
  "pytest-mock>=3.15.1",
  "ruff>=0.16.0",
  "pyinstaller>=6.21.0",
]

[tool.setuptools.dynamic]
version = {attr = "pytexmk.version.__version__"}

[tool.setuptools.package-data]
"pytexmk.data" = ["README.html", "*.toml"]
"pytexmk" = ["locale/**/*.mo", "locale/**/*.pot"]

[tool.ruff]
target-version = "py314"
line-length = 120

[tool.ruff.lint]
extend-ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --strict-markers"
markers = [
  "unit: 单元测试",
  "integration: 集成测试",
  "regression: 回归测试",
  "xindy: xindy 相关测试",
  "slow: 标记慢测试（真实编译）",
  "requires_latex: 需要 TeX 环境的测试",
]
